apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  namespace: prod-app
  annotations:
    cloud.google.com/backend-config: '{"default": "demo-backendconfig"}'
spec:
  type: NodePort
  selector:
    app: demo-app
  ports:
    - port: 80
      targetPort: 80
