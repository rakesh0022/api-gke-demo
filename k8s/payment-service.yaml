apiVersion: v1
kind: Service
metadata:
  name: payment-svc
  namespace: prod-app
  annotations:
    cloud.google.com/neg: '{"exposed_ports":{"80":{}}}'
spec:
  selector:
    app: payment-app
  ports:
  - port: 80
    targetPort: 8080
