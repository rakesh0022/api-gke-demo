openapi: 3.0.0
info:
  title: demo-api
  version: 1.0.0

# -----------------------------------------------------------------
# FIX: This section was missing or incorrectly indented in your file.
# It defines the "demo-backend" you referenced below.
# -----------------------------------------------------------------
x-google-api-management:
  backends:
    demo-backend:
      address: https://demo-cloudrun-api-648502722674.us-central1.run.app
      protocol: h2

paths:
  /:
    get:
      operationId: root
      # This tells API Gateway to look up "demo-backend" in the section above
      x-google-backend:
        address: demo-backend
      responses:
        '200':
          description: OK
